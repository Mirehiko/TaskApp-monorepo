export enum TaskStatus {
  DRAFT = 'draft',
  BACKLOG = 'backlog',
  PENDING = 'pending',
  IN_PROGRESS = 'inProgress',
  REVIEW = 'review',
  DONE = 'done',
  WONT_DO = 'wontDo',
  FAILED = 'failed'
  // BLOCKED_BY = 'blockedBy'
}
